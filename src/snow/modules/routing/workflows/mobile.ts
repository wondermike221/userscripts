import { errorNode, type Workflow } from '../workflow';

export const awfNewPhoneWorkflow: Workflow = {
  label: 'AWF New Phone',
  load: async () => errorNode('TODO: implement AWF new phone workflow'),
};

export const awfReplacementPhoneWorkflow: Workflow = {
  label: 'AWF Replacement Phone',
  load: async () => errorNode('TODO: implement AWF replacement phone workflow'),
};
